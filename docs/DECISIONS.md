# ByrdHouse DECISIONS

*Append-only log. One line per decision: date · decision · why. Never edit old lines.*

- 2026-07-10 · Treat U0 as functionally complete and move active work to U1 Image Lab; setup/status commands are gated by anti-loop rules · prevents future sessions from re-running machine setup instead of using the working belt
- 2026-07-10 · Built the full software side (U0–U3 + judge + dashboard) up front per founder request; unlocks still gate what gets USED and verified on hardware, but the code ships now so machines only need config + debugging · founder decision overrides build-in-order
- 2026-07-10 · Router implemented with Python stdlib (http.server + sqlite3) instead of FastAPI — same v2 §6 contract, zero pip installs on the machines; can swap frameworks later without changing routes · fewer failure modes on fresh Windows boxes
- 2026-07-10 · Until BYRD-MINI is set up, BYRD-GAMING hosts the router (startup.run_router=true); when MINI arrives: flip run_router false on GAMING, true on MINI, point services.router at byrd-mini, copy db/byrdhouse.db over · belt owner is MINI per blueprint, GAMING is a temporary host
- 2026-07-10 · Memory/Qdrant live on BYRD-MINI with sqlite_db=D:/ByrdHouse/db/byrdhouse_memory.db, sqlite_table=memories, qdrant_collection=byrdhouse_memories · keeps the ops memory stack off the gaming worker box
- 2026-07-10 · GitHub repo (redbooter0/Byrdhouse) is the source of truth for the U0 kit; machines sync FROM the repo via setup scripts · kills the "living doc drifts" problem with version control
- 2026-07-10 · Odysseus/smart-home/Stripe stack removed from repo; Cherry Studio remains the user-facing model GUI and ByrdHouse focuses on router/worker/dashboard/image/video/game/MCP belt · founder clarified Odysseus was not planned or needed
- 2026-07-09 · Realms/Godot weekend lane opens now (Cherry Studio + Godot MCP); belt integration waits for U6 · ROOM_MAP correction
- 2026-07-09 · Engine verdict: Godot (featherweight editor, headless CLI, MCP ecosystem); Unity only if mobile/asset-store need appears; Unreal parked · ROOM_MAP
- 2026-07-08 · ByrdHouse is a creator platform, not a mining platform; miners manual + seasonal · STATE direction
- 2026-07-08 · 5080 order: let expire — kept $1,456; hardware rule: cash only, no financing · Blueprint v3 Layer 0
- 2026-07-08 · Phases collapsed into unlocks U0–U6 + Frozen Backlog; numbered-phase prose retired · Blueprint v2
- 2026-07-08 · No new model downloads (freeze stands); no Redis/broker — SQLite WAL is the queue at this scale · Blueprint v2
- 2026-07-11 · Belt exposed as an MCP server (byrd_belt_mcp.py) so one shared tool roster drives it from any client on either machine; bot NEVER drives ComfyUI directly (mode ritual); autonomy = BYRD_BELT_MCP_READONLY permission flag, not a new build · founder dream: always-on operator on both PCs
- 2026-07-12 · Phase B Product Recovery Sprint: dashboard redesigned from 16 rooms to 3 tabs (Home/Create/Library) + hidden System panel; all backend behavior preserved, zero router/worker changes; no room earns main nav until real output has passed through it · founder identified the app became an admin console instead of a command center; the machinery was showing instead of being hidden
- 2026-07-12 · Identity profiles live under profiles/{id}/ with profile.json schema and references/ for face photos; reference photos gitignored (personal); worker auto-resolves face from profile dir when recipe has subject_profile · Creator V1 spec: recurring subject identity preservation via IP-Adapter FaceID
- 2026-07-13 · The GAMING anime target-edit lane uses a compact SD1.5-scale Meina base + optional LCM draft LoRA, with face-only img2img/inpaint and an owner-authorized identity LoRA; Flux2/SDXL and InsightFace swaps are excluded from the funded application lane · fits the 8 GB RTX 3070, preserves the belt, and avoids a non-commercial face-recognition dependency
- 2026-07-15 · Face swap goes through the belt as image.faceswap over ReActor (direct + style-blend two-pass), with identity LoRAs trained locally via kohya wrappers; training limits live in config (vram_budget_mb=7200, cpu_threads=16) and LoRA outputs are versioned files that never overwrite · founder needs local face swaps onto anime characters and repeatable identity training on the 8GB 3070; inswapper is non-commercial so monetized output uses the LoRA/FaceID generation route
- 2026-07-15 · Daily face-swap driver is the AUTO route (Impact Pack FaceDetailer: detect->mask->redraw-as-founder->composite) with zone-mask and ReActor blend as fallbacks; detector configurable via image.faceswap_detector · founder wants upload-and-done local swaps onto anime/game characters (Gojo, Vegeta, Luffy, Link) usable daily
- 2026-07-15 · CPU pre-step formalized as the PREVIEW route: detection-only graph archives zone overlay + soft mask as approvable artifacts, approved mask chains into the zone edit by artifact id · founder rule from the Codex sessions (GPU must not decide the mask; failures inspectable before GPU spends a step) implemented belt-idiomatically instead of hand-rolled landmark code
- 2026-07-15 · Added CPU crop preflight rerouting and adapter-level crop gate so expandable head/neck envelopes are expanded and re-audited before any GPU edit · validated on the hard Vegeta target and kept the belt suite green
- 2026-07-15 · Switched the face-zone v2 lane to support a CPU-only seed/composite finish and set the recipe default to skip GPU cleanup · the founder confirmed the CPU output looked better than the GPU cleanup on hard anime targets
- 2026-07-15 · Reconciled the two face lanes into one belt: Codex's CPU-first mesh-seed lane (recipe-runner face_zone_identity_edit, SD1.5 Meina, license-clean) is the QUALITY lane; preview/zone/auto routes are the fast daily tier; ReActor/FaceID routes are private-experiment-only per the funded-lane policy · both agents' work survives, one dispatch table, one test suite
- 2026-07-15 · Reconstructed the missing anime_face_zone_edit v2 recipe to the suite-locked two-pass spec (identity_fill 0.38 + line_harmonize 0.12, skip_gpu_cleanup default, accessory truth per preset); face-zone cleanup v3 is ControlNet-CANNY-guided from the identity mesh seed (recipe anime_face_zone_edit@3, denoise 0.55, openrail canny model) with target hair re-asserted OVER the likeness in the CPU composite · the v1 unguided pass made the GPU useless (low denoise kept seams, high destroyed identity); guidance lets the 3070 redraw the full zone including the forehead without losing Carey geometry — untested on hardware until the PC reruns Gojo/Vegeta
- 2026-07-15 · The examiner is the belt's first step for ANY uploaded image (byrdfacezone analyze): per-face operability verdicts + risk flags (extreme_expression/strong_profile/too_small) + the per-feature likeness plan (likeness where identity lives, target logic/shape/theme everywhere else); wired as a hard gate in edit_face_zone and as route examine on image.faceswap · founder contract: understand where it can and cannot operate BEFORE any GPU work, start small (geometry v1), semantic enrichment staged next
- 2026-07-15 · Operator layer: facelab.ps1 is the single manual entry point (preflight/examine/quality/zone/auto/swap/collect/train) with docs/FACE_OPS.md as the manual; thorough examination (scale-stability, occlusion truth, recommended lane, timed) is the default gate before any edit; the lane ladder codifies main plan + 5 backups so new ways get tested while the proven way keeps working · founder rules: real analysis effort before edits, 3-5 backup plans per task, commands Codex or the founder can run by hand
- 2026-07-15 · Flow fixes: quality-lane canvas follows the examiner-measured face (512/640/768 — no more softening large faces through a fixed 512), v3 lane batches 1-4 candidates through one submit with every candidate composited and carded, adapter no longer discards extra outputs; ladder auto-fallback conductor and group-photo fan-out staged · founder ask: the route the image takes must flow — remove detail loss and dead outputs from the path
- 2026-07-15 · Extra avenues replicated from proven 8GB community stacks, riding as per-job parameters (engine.workflow / facelab -Workflow) so the proven default never moves: A) DifferentialDiffusion seam-killer variant (core node, graded mask becomes a strength map), B) IP-Adapter PLUS FACE real-photo identity anchor (h94 Apache-2.0, CLIP-based — not insightface, funded-lane-eligible), D) PhotoMaker SDXL smoke kept as license-gated backup · founder rule: reverse-engineer what works on like setups; always covered with backup ways
- 2026-07-15 · ByrdCast Swap V0 build-report fixes: removed expression placeholder (was scoring 1.0 — V0 5-point landmarks can't measure expression, now recorded as unmeasured and renormalized away); made identities_root and outputs_root relative paths resolved against BYRDHOUSE_ROOT (were hardcoded E:/ByrdHouse); removed db WAL/SHM runtime files from git tracking · build report recommendation: honest unmeasured factors, portable config, clean repo
- 2026-07-15 · ByrdCast Swap V0 staged improvements (not yet implemented): merge geometric + semantic masking systems into one resolver; enforce GPU mode programmatically instead of printing a reminder; calibrate the 0.62 acceptance threshold with labeled 5-target examples on real hardware; make licensing a hard route gate (block non-commercial ReActor/inswapper routes for public output); freeze expansion until first 5-target visual proof passes · build report identified these as the next gates before V1
- 2026-07-16 · Adopted the 2026-07-15 Implementation Handoff as spec of record with live-state precedence: root config + flat scripts/workflows/recipes + CI contract checks stay as production protection (no directory restructure); candidate workflow byrd_identity_swap_v1 deferred until a valid ComfyUI export exists · the handoff's own rules: don't force the tree, candidate only after valid export
- 2026-07-16 · Identity Stack V1 (P1) plan: sequential isolated-lab installs — RMBG light models → facetools (prove zero-drift warp-back) → ReActor baseline → Forbidden Vision; FaceShaper/Fayens benchmark-only; InfiniteYou deferred (24GB+/license); InstaSwap/DeepFuze rejected; five-target benchmark (identity-benchmark.ps1) decides the winning branch, expected D but not assumed · handoff §6/§10 reconciled to the licensing lanes
- 2026-07-16 · LoRA lane: train-carey-meina-lora.ps1 (sd-scripts) stays incumbent; duck3244/lora-sd-custom is an isolated-env candidate that must beat it on the same fixed prompts; any new trainer starts conservative (SD1.5, 512, batch 1, rank 16, text encoder off, flip off, 50-step smoke then 400-800) · eight benchmarked incumbent runs of history; handoff §7
- 2026-07-16 · Governance made repo policy: every model/checkpoint/adapter needs a row in docs/model-license-manifest.md (license, lane, size/hash, rollback) BEFORE install or first use; agent access is tiered 0-4 per docs/agent-safety-policy.md, Tier 0 default, escalation = founder decision logged here · handoff §12 turned from prose into enforceable repo rules
- 2026-07-16 · ByrdCoder Local V0: OpenCode + opencode-lmstudio@0.3.1 (MIT, pinned stable over the 1.0 RC) is the local coding agent lane over the existing LM Studio — isolated via OPENCODE_CONFIG into gitignored LLM\byrdcoder\ with the URL substituted from byrdhouse.config.json at start (zero hardcoded hosts); seven byrd-* profiles default read-only with deny-by-default permissions; byrd-build is branch-guarded (never main, approved prefixes only) and can never push/merge · founder: coding must continue when Codex/cloud usage is exhausted, without touching LM Studio, the router, or memory
- 2026-07-16 · ByrdCoder primary model is chosen ONLY from the 7-task benchmark (byrdcoder-benchmark.ps1, disposable clone, measured + judged columns), never from chat feel; two-agent loop (byrdcoder_review.py) fails closed — unparseable/unreachable reviewer = block, and a blocked patch is never promoted automatically (promotion stays founder Tier 4) · Phase 5/6 of the ByrdCoder spec
- 2026-07-16 · Codex proxy NOT installed in ByrdCoder V0; may be evaluated later as an optional compatibility adapter in an isolated lab only after the independent OpenCode lane passes its 10-point acceptance checkpoint on GAMING · keep the local lane's independence provable before adding compatibility layers
- 2026-07-16 · ComfyUI MCP Role A: joenorton/comfyui-mcp-server (Apache-2.0) evaluated and re-implemented as the belt-native narrow executor byrd_comfy_mcp.py rather than forked wholesale — ByrdHouse already owns the queue/artifact/sidecar machinery, so its session registry, HTTP port, publish_asset, set_comfyui_output_root and persistent set_defaults are structurally absent; only recipes curated in configs/byrdcoder/approved_workflows.json run; stdio transport, router-token auth, BYRD_COMFY_MCP_READONLY=1 default; traversal + unmapped-override rejection contract-tested · the agent gets execution power without a second image pipeline
- 2026-07-16 · ComfyUI MCP Role B: artokun/comfyui-mcp pinned at 0.34.0 (commit 6a7ceeb9b578a149b0da65b43e0def708f0b3078, MIT; 0.35.0 exists — pin is deliberate) as the isolated workflow architect: compact tool mode, autoinstall/autoupdate off, no CivitAI/HF/Cloud tokens, lab-only COMFYUI_URL, disabled in the config until a separate Workflow Lab ComfyUI exists; read-only until the verification pass is recorded; graph edits only on copies in workflows/experiments/comfyui-mcp-lab/; promotion only through the 7-gate checklist in workflows/candidates/README.md · full graph-editing power stays fenced away from production
- 2026-07-19 · Added acceptance_check() to byrdfacezone (new "accept" CLI subcommand) and wired it into edit_face_zone() output path: post-generation check on the FINAL composite (not the target) flags no_face_detected, face_cropped_top/bottom/left/right, face_too_small, multiple_faces; saves a padded face_crop_preview beside every output; result block on every sidecar card; docs/REPO_COMPARISON.md documents the full adopt/reject/revisit verdict for 8 external repos and specifies Create Room, Identity Acceptance Layer, and Media Finisher modules · founder pain point: "face detection wrong or incomplete, more of the face needs to be in frame" — examiner was checking the input only, not the output
- 2026-07-19 · Dashboard patch: artCard() renders green "face OK" badge or yellow flag badge from output_acceptance block; "rerun" button copies facelab.ps1 quality command from reproduce block; Face Swap panel adds seed field with Lock/Random controls; submitSwap() wires seed into job payload; all wired through window._artMeta lookup so no extra API calls · ZPix pattern: seed lock + gallery metadata reuse surfaced in the UI without any backend changes
